let trafficlights;
trafficlights = prompt("Enter the traffic signal color (Red, Yellow, Green): ");
if (trafficlights === "Red"){
    alert("Must stop.");
}

else if(trafficlights === "Yellow"){
  alert("Ready to move.")
}

else if(trafficlights === "Green"){
  alert("Move now.")
}